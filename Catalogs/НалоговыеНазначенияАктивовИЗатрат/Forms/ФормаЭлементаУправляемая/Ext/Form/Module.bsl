&НаКлиенте
Процедура ПриОткрытии(Отказ)
    Если Объект.Предопределенный Тогда
        Отказ = Истина;
		ТекстСообщения = НСтр("ru='Редактирование предопределенных элементов справочника ""Налоговые назначения активов и затрат"" не предусмотрено'");
		ПоказатьПредупреждение(Неопределено, ТекстСообщения);
	КонецЕсли;
КонецПроцедуры
